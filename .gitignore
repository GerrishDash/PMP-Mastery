# Exclude large PMBOK source markdown files
*.md

# Keep the walkthrough and task files if needed, but since they are in the appDataDir brain directory, they won't be in this folder anyway.
# We only want to deploy index.html, styles.css, app.js, manifest.json, sw.js, and pmp_logo.png.
